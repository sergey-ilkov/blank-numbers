<div class="decor-line-numbers">
    <span class="decor-line-number">{{ __('base.decorNumbers') }}</span>
    <span class="decor-line-number">{{ __('base.decorNumbers') }}</span>
    <span class="decor-line-number">{{ __('base.decorNumbers') }}</span>
    <span class="decor-line-number">{{ __('base.decorNumbers') }}</span>
    <span class="decor-line-number">{{ __('base.decorNumbers') }}</span>
    <span class="decor-line-number">{{ __('base.decorNumbers') }}</span>
    <span class="decor-line-number">{{ __('base.decorNumbers') }}</span>
    <span class="decor-line-number">{{ __('base.decorNumbers') }}</span>
</div>